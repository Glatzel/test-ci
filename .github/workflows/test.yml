on:
    workflow_dispatch:
jobs:
  example:
    runs-on: ubuntu-latest
    steps:
    - run: echo "Test"
    - run: exit 0
      id: bad
    - run: echo "OK"
      if: job.steps.bad.status == failure() && (failure()||success())
