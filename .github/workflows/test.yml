on:
    workflow_dispatch:
jobs:
  example:
    runs-on: [self-hosted]
    steps:
      - run: echo "VARI=true" >> "$env:GITHUB_ENV"
  example1:
    runs-on: windows-latest
    steps:
      - run: Write-Output "$env:VARI"
